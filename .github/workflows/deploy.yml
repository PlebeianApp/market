name: Build and Deploy Bun Project
on:
  push:
    branches:
      - master
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: preview
    steps:
      - uses: actions/checkout@v3
      - uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest
      - run: bun install
      - run: bun run build
      - name: Deploy to VPS
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.REMOTE_HOST_TEST }}
          username: ${{ secrets.REMOTE_USER_TEST }}
          password: ${{ secrets.REMOTE_PASSWORD_TEST }}
          source: './*'
          target: '~/market'
          rm: true
      - name: Write .env file from secrets
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.REMOTE_HOST_TEST }}
          username: ${{ secrets.REMOTE_USER_TEST }}
          password: ${{ secrets.REMOTE_PASSWORD_TEST }}
          script: |
            cat <<EOF > ~/market/.env
            APP_RELAY_URL=${{ secrets.APP_RELAY_URL_TEST }}
            APP_PRIVATE_KEY=${{ secrets.APP_PRIVATE_KEY_TEST }}
            EOF
      - name: Restart market-dev.service
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.REMOTE_HOST_TEST }}
          username: ${{ secrets.REMOTE_USER_TEST }}
          password: ${{ secrets.REMOTE_PASSWORD_TEST }}
          script: |
            sudo /bin/systemctl restart market-dev.service