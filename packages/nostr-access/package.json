{
	"name": "@plebeian/nostr-access",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "The environment layer: one interface for reading Nostr, with one binding per host (app, CMS, napplet). The only layer that touches the world.",
	"main": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"sideEffects": false,
	"dependencies": {
		"@plebeian/product-event": "0.1.0"
	}
}
